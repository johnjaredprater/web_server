name: Docker Image CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:

  build-and-push:

    runs-on: ubuntu-latest

    steps:
    - 
      uses: actions/checkout@v4
    -
      name: Use Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '22.x'
    - 
      run: npm ci
    - 
      run: npm run build --if-present
    - 
      run: npm test
    -
      name: Set up QEMU
      uses: docker/setup-qemu-action@v3
    -
      name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3
    - 
      name: Login to GitHub Container Registry
      uses: docker/login-action@v3
      with:
        # registry: registry-1.docker.io
        username: johnjaredprater
        password: ${{ secrets.DOCKER_ACCESS_TOKEN }}
    # - name: Login to Docker
    #   run: docker login registry-1.docker.io -u johnjaredprater -p ${{ secrets.DOCKER_ACCESS_TOKEN }}
    -
      name: Build and push
      uses: docker/build-push-action@v6
      with:
        context: .
        push: true
        tags: johnjaredprater/web_server:latest
    # - name: Build the Docker image
    #   run: docker build . --tag webserver:$(date +%s)
    # - name: Push the Docker image
    #   run: docker push johnjaredprater/web_server:latest
